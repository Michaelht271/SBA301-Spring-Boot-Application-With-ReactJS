server:
  port: 8080

spring:
  application:
    name: pe-hsf302-de180051

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=PeHsf302De180051;encrypt=true;trustServerCertificate=true;
    username: sa
    password: MichaelAn271
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServer2012Dialect
        format_sql: true

  thymeleaf:
    enabled: true
    prefix: classpath:/templates/
    suffix: .html
    cache: false