<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- ================= DATABASE CONFIGURATION ================= -->

        <!-- SQL Server Dialect (khuyên dùng 2012 trở lên) -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.SQLServerDialect
        </property>

        <!-- JDBC Driver -->
        <property name="hibernate.connection.driver_class">
            com.microsoft.sqlserver.jdbc.SQLServerDriver
        </property>

        <!-- Connection URL -->
        <property name="hibernate.connection.url">
            jdbc:sqlserver://localhost:1433;
            databaseName=LibraryDB;
            encrypt=true;
            trustServerCertificate=true
        </property>

        <!-- Username -->
        <property name="hibernate.connection.username">sa</property>

        <!-- Password -->
        <property name="hibernate.connection.password">Michaelht271</property>


        <!-- ================= HIBERNATE SETTINGS ================= -->

        <!-- Show SQL in console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Format SQL -->
        <property name="hibernate.format_sql">true</property>

        <!-- Auto create / update table -->
        <!-- Options: none | validate | update | create | create-drop -->
        <property name="hibernate.hbm2ddl.auto">update</property>


        <!-- ================= ENTITY MAPPING ================= -->

        <mapping class="com.michael.lab7.pojos.Book"/>
        <mapping class="com.michael.lab7.pojos.Student"/>

    </session-factory>
</hibernate-configuration>