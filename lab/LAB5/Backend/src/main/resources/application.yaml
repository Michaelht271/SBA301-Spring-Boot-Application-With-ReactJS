server:
  port: 8080

spring:
  application:
    name: pe-hsf302-de180051

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=LAB5;encrypt=true;trustServerCertificate=true;
    username: sa
    password: MichaelAn271
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true
